#spring:
#  application:
#    name: ifm-service
#  datasource:
#    url: jdbc:h2:mem:testdb
#    username: sa
#    password: password
#    driver-class-name: org.h2.Driver
#  jpa:
#    hibernate:
#      ddl-auto: update
#    show-sql: true
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.H2Dialect
#        format_sql: true
#  h2:
#    console:
#      enabled: true
#      path: /h2-console
#  servlet:
#    multipart:
#      max-file-size: 100MB
#      max-request-size: 100MB
#
## ✅ logging 和 mybatis 不属于 spring 内部配置，所以要与 spring 平级
#logging:
#  level:
#    org.mybatis: DEBUG           # MyBatis 框架日志
#    java.sql: DEBUG              # JDBC 操作日志
#    java.sql.Statement: DEBUG    # 打印实际 SQL 语句
#    java.sql.PreparedStatement: DEBUG  # 打印参数绑定信息
#    java.sql.ResultSet: DEBUG    # 打印结果集
#    com.yaoyiduo.quiz.mapper: DEBUG  # 打印你自己包里的 Mapper 执行日志
#
#mybatis:
#  configuration:
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl  # ✅ 注意大小写
#
#audio:
#  upload-path: ${user.dir}/uploads
#
#
server:
  port: 8080

spring:
  application:
    name: ifm_service

  datasource:
    url: jdbc:mysql://localhost:3306/ifm_service?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
#    password: "00000712"
    password: "xiesuyang888"

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB



audio:
  upload-path: ${user.dir}/uploads
