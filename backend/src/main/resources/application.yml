server:
  port: 8080

spring:
  application:
    name: ifm_service

  datasource:
    url: jdbc:mysql://localhost:3306/ifm_service_v3_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8&useUnicode=true
    username: root
    password: "kxyzgj731105"

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        show_sql: true
    # 添加以下配置
    database-platform: org.hibernate.dialect.MySQLDialect


  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB



audio:
  upload-path: ${user.dir}/uploads

logging:
  level:
    org.springframework.web: DEBUG
    org.hibernate: DEBUG