# iFM播客项目前后端联调指南

## 1. 项目架构概述

### 1.1 后端架构
- **技术栈**：Node.js + Express + MongoDB + Redis
- **API前缀**：`/api`
- **主要接口**：
  - 认证：`/api/auth/register`、`/api/auth/login`
  - 用户：`/api/users/me`
  - 上传：`/api/upload`
  - 群组：`/api/group`

### 1.2 前端架构
- **技术栈**：HTML + CSS + JavaScript
- **页面结构**：单页面应用(SPA)风格，通过路由跳转
- **API调用**：使用Fetch API进行异步请求

## 2. 联调准备工作

### 2.1 环境配置

#### 2.1.1 后端环境
- **Node.js**：确保已安装Node.js 14+版本
- **MongoDB**：
  - 安装MongoDB并启动服务
  - 配置数据库连接地址：`mongodb://localhost:27017/ifm`
- **Redis**：
  - 安装Redis并启动服务
  - 配置Redis连接地址：`redis://localhost:6379`

#### 2.1.2 前端环境
- 确保前端页面能正常访问后端API
- 检查跨域配置是否正确

### 2.2 依赖安装

```bash
# 安装后端依赖
npm install

# 安装前端依赖（如果有）
npm install
```

### 2.3 启动服务

```bash
# 启动后端服务
npm start

# 前端页面可通过浏览器直接访问
```

## 3. 接口联调步骤

### 3.1 认证接口联调

#### 3.1.1 注册接口 (`/api/auth/register`)

**请求参数**：
```json
{
  "username": "用户名",
  "email": "邮箱/手机号",
  "password": "密码"
}
```

**响应示例**：
```json
{
  "message": "User registered successfully",
  "user": {
    "id": "user_id",
    "username": "用户名",
    "email": "邮箱/手机号"
  }
}
```

#### 3.1.2 登录接口 (`/api/auth/login`)

**请求参数**：
```json
{
  "email": "邮箱/手机号",
  "password": "密码"
}
```

**响应示例**：
```json
{
  "token": "jwt_token"
}
```

### 3.2 用户接口联调

#### 3.2.1 获取当前用户 (`/api/users/me`)

**请求头**：
```
Authorization: Bearer jwt_token
```

**响应示例**：
```json
{
  "id": "user_id",
  "username": "用户名",
  "email": "邮箱/手机号"
}
```

## 4. 存在的问题及解决方案

### 4.1 数据库连接问题

**问题**：MongoDB和Redis连接失败，导致API调用超时

**解决方案**：
1. 启动MongoDB服务：
   ```bash
   # Windows
   net start MongoDB
   
   # Linux
   sudo systemctl start mongod
   ```

2. 启动Redis服务：
   ```bash
   # Windows
   redis-server.exe
   
   # Linux
   sudo systemctl start redis-server
   ```

3. 或修改配置文件，禁用数据库连接检查：
   - 在`src/config/database.js`中，将连接失败时的`process.exit(1)`改为日志记录
   - 在`src/config/redis.js`中，保持连接失败时不退出应用的配置

### 4.2 前后端数据字段不统一

**问题**：前端使用手机号登录，但后端User模型只接受email字段

**解决方案**：

1. **方案一**：修改后端模型，添加phone字段
   ```javascript
   // src/models/User.js
   const UserSchema = new mongoose.Schema({
     // ...
     phone: {
       type: String,
       unique: true,
       match: [/^1[3-9]\d{9}$/, 'Please enter a valid phone number']
     },
     // ...
   });
   ```

2. **方案二**：前端将手机号作为email字段发送（临时方案）
   - 保持现有代码不变，将手机号填充到email字段中

### 4.3 前端API调用路径问题

**问题**：前端API调用路径是否正确

**解决方案**：
- 确认API前缀是否为`/api`
- 检查请求方法是否正确（GET/POST/PUT/DELETE）
- 检查请求头是否正确设置（Content-Type, Authorization等）

## 5. 接口测试方法

### 5.1 使用PowerShell测试API

```powershell
# 测试注册接口
Invoke-RestMethod -Uri http://localhost:3000/api/auth/register -Method POST -ContentType "application/json" -Body '{"username":"testuser","email":"test@example.com","password":"password123"}'

# 测试登录接口
Invoke-RestMethod -Uri http://localhost:3000/api/auth/login -Method POST -ContentType "application/json" -Body '{"email":"test@example.com","password":"password123"}'

# 测试获取当前用户接口（需要替换token）
Invoke-RestMethod -Uri http://localhost:3000/api/users/me -Method GET -Headers @{"Authorization"="Bearer your_jwt_token"}
```

### 5.2 使用Postman测试API

1. 下载并安装Postman
2. 创建测试集合
3. 添加测试请求
4. 配置请求参数和头信息
5. 发送请求并查看响应

## 6. 前端联调注意事项

### 6.1 跨域问题

- 后端已配置CORS中间件，允许所有来源的请求
- 如果遇到跨域错误，检查浏览器控制台错误信息

### 6.2 认证Token处理

- 登录成功后，将Token存储在localStorage中
- 发送请求时，在请求头中添加Authorization字段

```javascript
// 存储Token
localStorage.setItem('authToken', data.token);

// 发送带Token的请求
fetch('http://localhost:3000/api/users/me', {
  method: 'GET',
  headers: {
    'Authorization': `Bearer ${localStorage.getItem('authToken')}`
  }
})
```

### 6.3 错误处理

- 添加适当的错误处理逻辑
- 显示友好的错误提示

```javascript
async function handleLogin() {
  try {
    const response = await fetch('http://localhost:3000/api/auth/login', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        email: loginPhone.value.trim(),
        password: loginPwd.value
      })
    });
    
    const data = await response.json();
    
    if (response.ok) {
      // 登录成功处理
    } else {
      // 登录失败处理
      alert(`登录失败: ${data.message || '用户名或密码错误'}`);
    }
  } catch (error) {
    console.error('登录请求失败:', error);
    alert('登录失败，请检查网络连接或稍后重试');
  }
}
```

## 7. 联调测试流程

1. **启动后端服务**：`npm start`
2. **测试API接口**：使用PowerShell或Postman测试各接口
3. **修复接口问题**：解决API调用中遇到的问题
4. **启动前端页面**：打开前端HTML页面
5. **测试前端功能**：测试登录、注册等功能
6. **修复前端问题**：解决前端调用中遇到的问题
7. **完整流程测试**：从登录到使用各功能的完整流程测试

## 8. 常见问题排查

### 8.1 API调用返回404
- 检查API路径是否正确
- 检查HTTP方法是否正确

### 8.2 API调用返回401
- 检查Token是否有效
- 检查Token是否正确添加到请求头

### 8.3 API调用返回500
- 检查服务器日志
- 检查数据库连接
- 检查代码错误

### 8.4 前端页面无法访问
- 检查文件路径是否正确
- 检查服务器是否启动

## 9. 后续优化建议

1. **添加API文档**：使用Swagger或Postman生成API文档
2. **添加测试用例**：编写单元测试和集成测试
3. **优化错误处理**：统一前后端错误格式
4. **添加日志系统**：记录API调用和错误信息
5. **实现数据验证**：加强前后端数据验证

---

**联调负责人**：
**联调时间**：
**联调状态**：进行中
